{
  "name": "personal-vitepress-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "VITEPRESS_BASE=/ vitepress dev docs --host 0.0.0.0",
    "build": "vitepress build docs",
    "build:server": "VITEPRESS_BASE=/ vitepress build docs",
    "preview": "vitepress preview docs --host 0.0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "vitepress": "^1.6.4"
  }
}
